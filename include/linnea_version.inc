; The version string, in ONE place. The Makefile derives its VERSION from this
; line (and writes release/VERSION from that), and both binaries print it for
; --version, so the number never drifts across them. Bump it here to release a
; new version.
%define LINNEA_VERSION "1.1.0"
